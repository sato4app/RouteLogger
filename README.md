## RouteLogger - ハイキング等の移動経路をGPSに記録。写真も記録。

### 実装機能

  - 地図の初期表示と、データのCloud保存以外は、ネットワーク回線に接続せずに動作可能。
  - 移動の経路をGPSに記録。移動の手段は問わない。(ハイキング、散歩、サイクリング等)
  - 経路や写真は、ローカル(indexedDB)に一時的に保存。
  - 保存データは、Cloud(firebase:ユーザー登録が必須)または、kmz形式でファイル出力可能。